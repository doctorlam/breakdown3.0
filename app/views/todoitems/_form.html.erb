<%= form_for([@project, @milestone, @milestone.todoitems.build]) do |f| %>
	<%= f.text_field :description, placeholder: "New task", class: 'form-control' %>
	<br>
	<%= f.submit "Add Task", class: 'btn btn-primary' %>
<% end %>