<div class="milestones-form">
<%= form_for([@project, @project.milestones.build]) do |f| %>
	
	<%= f.text_field :content, placeholder: "Add a Milestone", class: 'form-control' %>
	<br>
	<%= f.submit "CREATE NEW MILESTONE", class: 'btn btn-primary' %>
<% end %>

</div>
<br>
<br>
<div class="btn-done">
<%= link_to 'Done', projects_path, class: 'btn btn-primary' %>
</div>